# Docker example

This is a small client-server application used for educational purpose.

## Install

Build the images

```bash
docker-compose build
```

Run the services
```bash
docker-compose up
```
